# myxo-project

`BiharmonicSolverFourier.wl` solves fluid dynamics without viscosity or Q fluctuations

`covariance visualisation.nb` visualizes covariance matrix as an ellipse, also rotational transformations

`g-python.nb` same as g, but  uses results generated by python code

`g.nb` solving equations, and visualisation

`Gdef.wl` definition of function G

`Q-autocorr-calc.nb` 

`Q-field and fluctuations.nb` visualisation of the nematic field
`remote_kernel.nb`
`remote_solver.nb`
